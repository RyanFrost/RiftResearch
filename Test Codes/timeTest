
#include <ctime>



int main()
{

	std::time_t t1,t2;
	
	double seconds;
	
	time(&t1);

	for (int i = 0; i<1000000;i++)
		i = exp(i,2);

	time(&t2);

	seconds = difftime(t1,t2);

	printf("%f seconds\n",seconds);

	return 0;
}
